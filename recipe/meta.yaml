{% set name = "sqlfluff" %}
{% set version = "0.6.9" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/sqlfluff-{{ version }}.tar.gz
  sha256: 799f9d801f4cfb6b7214c1d6af9c5fcf79f8e3de778094d540afa488bc3e1fd1

build:
  noarch: python
  number: 0
  entry_points:
    - sqlfluff = sqlfluff.cli.commands:cli
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - appdirs
    - cached-property
    - click >=7.1
    - colorama >=0.3
    - configparser
    - dataclasses
    - diff-cover >=2.5.0
    - jinja2
    - oyaml
    - pathspec
    - pluggy
    - pytest
    - python >=3.6
    - tblib
    - toml
    - typing-extensions

test:
  imports:
    - sqlfluff
    - sqlfluff.api
  commands:
    - pip check
    - sqlfluff --help
  requires:
    - pip

about:
  home: https://www.sqlfluff.com/
  summary: The SQL Linter for Humans
  dev_url: https://github.com/sqlfluff/sqlfluff
  doc_url: https://docs.sqlfluff.com/en/stable/index.html
  license: MIT
  license_file: LICENSE.md
  description: |
    SQLFluff is a dialect-flexible and configurable SQL linter. Designed
     with ELT applications in mind, SQLFluff also works with Jinja
     templating and dbt. SQLFluff will auto-fix most linting errors,
     allowing you to focus your time on what matters.

extra:
  recipe-maintainers:
    - thewchan
